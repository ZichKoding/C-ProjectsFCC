int firstValue = 500;
int secondValue = 600;
int largerValue;

// Math's method Max() is an overloaded method with 13 options. 
largerValue = Math.Max(firstValue, secondValue);

Console.WriteLine(largerValue);