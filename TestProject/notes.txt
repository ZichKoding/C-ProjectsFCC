To build and run in the terminal, `dotnet build` and `dotnet run`.

to create a new console application in a specified folder, type dotnet new console -o ./CsharpProjects/TestProject and then press Enter.